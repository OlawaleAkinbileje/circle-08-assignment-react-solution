import React from 'react';

const Header = () => {
    return (
        <header className="site-logo">
            <img className="logo" src="/assets/Logo.svg" alt='Spots Logo' />
        </header>
    );
};

export default Header;